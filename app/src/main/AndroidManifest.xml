<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectaac">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".LoadingActivity"></activity>
        <activity android:name=".CPMenuActivity" />
        <activity android:name=".Location_recommend" />
        <activity android:name=".TTS" />
        <activity android:name=".option" />
        <activity android:name=".MakeSymbolActivity" />
        <activity android:name=".CpChangeActivity" />
        <activity android:name=".CommunicationPlate" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDQqSsniONgFXbL-EF-R88aYaXXzW1KsLw" />
    </application>

</manifest>